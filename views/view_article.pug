extends layout

block content
  h1(align='center') #{article.title}
  h3(align='center') written by #{article.author}
  article(align='center') #{article.body}
  hr
  a.btn.btn-default(href='/articles/edit/'+article._id) Edit
  a.btn.btn-danger.delete-article(href='#', data-id=article._id) Delete
